subtract – subtracts value in r1 from r0. Takes 2 integer operands
addition – adds values in r1 and r0. Takes 2 integer operands
exponent – raises r0 to the power of r1. Takes 2 integer operands
divide – divides r0 by r1, and floors the result. Takes 2 integer operands
bitcount – counts number of set bits in r0. Takes 1 integer operand
Sum – Sums all the numbers from r1, up to r0. Takes 2 integer operands
factorial – computes factorial of r0. Takes 1 integer operand.

